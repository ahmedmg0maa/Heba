# PROJECT STATE
Last session: 2026-08-27 | Current program: Code X development-first execution | LOCAL DEVELOPMENT IN PROGRESS — STAGING EXTERNAL GATE remains for recovery, provider migration and live acceptance only

## P1 governed Resource Hub checkpoint — 2026-08-27
- `/resources` and `/resources/[slug]` now provide a real published-only Hub for articles/guides, video and podcast with topic/type/duration filters, truthful empty behavior, canonical metadata, schema.org output, accessible transcript/captions, safe external-source disclosure and a published related-offer link.
- Local migration 051 adds `resources`, `resources.manage`, public/Admin RLS, scheduling and atomic service-only create/update/delete RPCs. Drafts may remain incomplete and private, while scheduled/published resources enforce usable article content or media source/duration, transcript/captions, 046 public-media rights and a published related product.
- `/admin/resources` has structured Arabic fields, draft/schedule/publish/archive states, media/related-product selection, SEO, saved-card preview, rights readiness and safe deletion only after archive. All writes recheck permission inside PostgreSQL and audit only structural flags—not title, URLs, body or transcripts.
- Arabic search now includes only published Resource Hub rows and searches title/excerpt/topic plus a bounded transcript prefix. Sitemap covers the Hub and published detail slugs. The governed Home registry can add/order/revise/preview/publish a featured-resources section which disappears with no records.
- The complete `pnpm check:deploy` gate passed: isolated type-check/lint, the 66-page production build, public Playwright **64/64** across desktop/mobile, Resource/Search/Home executable contracts and every archive/delivery/booking/CMS/security/Admin/media/commerce/catalog/database/launch audit. Migration 051 is source-only and has not been applied externally.

## P1 governed Press checkpoint — 2026-08-27
- `/press` now renders only published, due source records with HTTPS original links, factual dates and an explicit classification separating independent editorial coverage from partnerships, owned channels and events. Type filters, external-link disclosure and a truthful no-source empty state are included; no logo, quote or authority claim was invented.
- Local migration 050 adds the forward-only `press_mentions` domain, `press.manage` mappings for Admin/content/marketing roles, public-read/Admin-manager RLS, rights-aware image publication, scheduled publishing and service-only atomic create/update/delete RPCs. Published/scheduled rows must be archived before physical deletion; every mutation writes a metadata-only audit record in the same transaction.
- `/admin/press` performs a page-level permission check, provides structured Arabic CRUD/schedule/feature fields, displays media-rights readiness and a real saved-card preview. Public images are accepted only from `public-media` with 046 rights evidence; the public consumer repeats that fail-closed filter.
- The governed Home section registry can now add, order, revise, preview, publish or hide a Press section. It renders only featured published source rows and disappears when none exist.
- The complete `pnpm check:deploy` gate passed: isolated type-check/lint, the 64-page production build, public Playwright **60/60** across desktop/mobile, Contact/Testimonials/Press executable contracts and every archive/delivery/booking/CMS/security/Admin/media/commerce/catalog/database/launch audit. Migration 050 remains source-only and has not been applied to any provider.

## P1 consent-complete Testimonials checkpoint — 2026-08-27
- `/testimonials` is now a dedicated Arabic/RTL public consumer with real type filters and a truthful empty state. It renders only reviews whose product remains published and whose record proves paid entitlement, explicit publication consent and Admin approval; absence of a name-consent flag produces the neutral label `عميلة موثقة` rather than exposing profile identity.
- The customer order dashboard now requires separate publication and first-name-display consent, validates bounded text, and explains withdrawal through the privacy/contact route. Migration 049 verifies a non-revoked entitlement linked to a paid order, stores only the first name when separately consented, resets resubmissions to pending, and audits flags/source without testimonial text or identity.
- Review approval, rejection, archive/restore, feature/unfeature and published owner response now run through one service-role-only RPC that rechecks `reviews.manage`, locks the row, requires verification/publication evidence for public actions, and writes the privacy-minimized audit in the same transaction. Admin Reviews displays source, shortened internal reference, consent and verification evidence before allowing moderation.
- Homepage social proof uses the same verification/consent boundary, and the public page is included in route/sitemap/mobile E2E contracts. The complete `pnpm check:deploy` gate passed: isolated type-check/lint, the 62-static-page-plus-dynamic-route production build, public Playwright **56/56** across desktop/mobile, both Contact/Testimonials executable contracts and every archive/delivery/booking/CMS/security/Admin/media/commerce/catalog/database/launch audit.
- Migration 049 remains source-only. Existing approvals are deliberately not treated as proof of customer publication consent, and no fake/backfilled consent or testimonial was created.

## P0 governed Contact intake checkpoint — 2026-08-27
- Public Contact submission is now server-mediated. The browser no longer inserts into `contact_messages`; a Server Action validates the governed purpose, normalized contact fields, explicit privacy consent and a honeypot before invoking a service-role-only database RPC.
- Forward-only local migration 048 removes the anonymous/authenticated insert path, adds purpose/consent metadata, and implements atomic per-device and per-email throttling without retaining raw IP addresses or user-agent strings. Submission and audit writes share one transaction, and audit metadata contains only purpose/source rather than message or contact PII.
- Admin Inbox mutations now use one database RPC that rechecks `inbox.manage`, locks the message, applies status/priority/assignment/note/reply changes, writes the outbox entry when delivery is configured, and appends a privacy-minimized audit record atomically. The existing Inbox UI remains the real consumer and now exposes purpose and recorded consent.
- The complete `pnpm check:deploy` gate passed: isolated type-check/lint, the 61-static-page-plus-dynamic-route production build, public Playwright **52/52** across desktop/mobile, the executable Contact contract, and every archive/delivery/booking/CMS/security/Admin/media/commerce/catalog/database/launch audit. Migration 048 is source-only and has not been applied to Supabase; provider persistence/RLS/rate-limit evidence remains part of `STAGING EXTERNAL GATE`.
- Exact next local task after the local commit is the next missing vertical slice from the development plan. No Production, DNS, provider, secret or external migration action is included.

## Production read-only recovery preflight checkpoint — 2026-08-27
- A sanitized Supabase Management API read now proves exactly one healthy authorized Production project with ref `zfbwpubsnuijybxjuidc`, no accessible non-Production project, and zero leftover `hebaelsherif-restore-*` projects. No account ID, connection address, key or password was printed.
- `HEBA_LAUNCH_PRODUCTION_DATABASE_URL` is absent from the Process, Windows User and Windows Machine scopes visible to this executor. Therefore no PostgreSQL connection, backup artifact or disposable restore target was created. Staging database connection variables are also absent, so 044–047 remain unapplied.
- The Free-project capacity issue is cleared because only Production remains accessible. Recovery was explicitly deferred by the owner as `STAGING EXTERNAL GATE`; no disposable restore or replacement Staging project was created. The bundled vanilla PostgreSQL server is not substituted as proof of a full Supabase restore because it lacks platform Auth/Storage/custom-extension parity.
- Recovery was hardened locally so an independent `--preflight-only` query runs before target creation and forces `default_transaction_read_only=on`, timeouts and the exact tenant-qualified Production Session-pooler identity. The same read-only enforcement applies to the logical dump source. Restore remains impossible until a newly created target is uniquely identified outside the pre-run project set.
- Fresh development baseline passes: `pnpm check:deploy` completed type-check, lint, isolated 62-route build, public Playwright **52/52**, and every local security/Admin/media/commerce/catalog/booking/database/launch audit. The earlier legacy fixture command stopped at its first read before any fixture write and is excluded from evidence.
- This checkpoint supplied the baseline for the now-implemented governed Contact intake slice. Recovery, new Staging creation, provider migration application and live acceptance remain a separate `STAGING EXTERNAL GATE` and do not stop local development.

## Code X local execution checkpoint — 2026-08-27
- The active primary is the current `D:/claude` Git worktree on `codex/master-merge-2026-08-27`, baseline HEAD `57820ab`. The missing requested archive name was not guessed: `D:/0000/hebaelsherif.zip` was proven to contain all 413 baseline tracked source files byte-for-byte but also unsafe private/generated material, so it was not extracted. Comparison archives were idea sources only and no application was replaced.
- Homepage, published-only Arabic search, structured `start-here`, catalog publication completeness, secure report export, revision recovery, responsive preview and privacy-safe Admin agenda views are implemented locally through fixed schemas/actions/permissions/audits and real public/customer consumers. Migrations 048–051 now own the governed Contact, Testimonials, Press and Resource Hub contracts; the remaining formal assessment-version domain must use a new forward-only number after 051 and owner-approved facts. No decorative or fake data substitute was created.
- Booking, manual-payment/entitlement and protected-delivery local contracts pass. They remain explicitly distinct from Staging RLS/RPC/Auth/Storage/provider evidence because the separate Supabase Staging project is not connected in this executor and 044–047 were not applied.
- `pnpm check:deploy` passed after the current changes: isolated type/build, lint, 62-page build, **52/52** ordinary public Playwright and all source/security/Admin/media/commerce/catalog/booking/database/launch audits. `pnpm test:e2e:cloudflare` initially exposed a real hydration-timing race in the start-journey keyboard test; after a client-readiness signal and deterministic wait, the rebuilt Worker suite passed **52/52** with retries disabled.
- `pnpm package:release` then passed a recursive **463-file** local archive inspection with no secret/build/test artifact, followed by a final `pnpm audit:security` pass. The archive stays local and is not a deployment or publication artifact.
- Evidence: `docs/SOURCE_OF_TRUTH_AND_INTAKE_2026-08-27.md`, `docs/MASTER_COMPARISON_AND_MERGE_PLAN_2026-08-27.md`, `docs/SUPABASE_STAGING_SCHEMA_RLS_AUTH_EVIDENCE_2026-08-27.md`, `docs/CLOUDFLARE_WORKERS_STAGING_RUNTIME_EVIDENCE_2026-08-27.md`, `docs/END_TO_END_CUSTOMER_AND_ADMIN_ACCEPTANCE_2026-08-27.md`, and `docs/FINAL_PREPRODUCTION_AUDIT_AND_SCORE_2026-08-27.md`.
- Status remains **`BLOCKED`**, official evidence score **59/100**. The first database password was disclosed in chat and is treated as compromised; it was not used. The direct database endpoint is IPv6-only and did not resolve from this executor. Exact next task: rotate the database password, then run `scripts/run-launch-recovery-drill-interactive.ps1` locally with the new **Session pooler** template (port 5432) and hidden new password. The runner reuses that IPv4 pooler for its disposable restore target and clears the secret. No push, remote Worker update, Production, DNS or migration write occurred.

## Cloudflare Staging deployment checkpoint — 2026-08-26
- A separate Workers.dev-only Worker, `heba-elsherif-platform-staging`, is deployed from commit `6be490a7912d685e2c090600e5a4eb5dfae97ed2` on `codex/cloudflare-compatibility-spike`. It uses `HEBA_DEPLOYMENT_ENV=staging`, a separate Worker name, `nodejs_compat`, and only the static `ASSETS` binding. No DNS, domain, Production Worker, Production Supabase, migration, or production setting changed.
- The deployed endpoint intentionally returns Basic-Auth `401` until Cloudflare Staging secrets exist. It proved noindex/CSP/HSTS/nosniff behavior and has no deployed source maps. The isolated local Worker suite now starts its own Wrangler server and passed **46/46** desktop/mobile tests; type-check, lint and the isolated Staging build passed.
- The two Staging Basic Auth secret names have since been added through Wrangler's interactive prompt, without reading or recording their values. An authenticated live HTTP pass returned `200` from eleven public/login routes and the expected `307` from `/admin`; CSP and noindex remain present. Live browser, Storage and write-journey evidence remains distinct and unverified.
- A separate Supabase Nano Staging project was created successfully with no copied Production data and an ephemeral generated database password that was neither printed nor stored in the repository. It is intentionally still blank: no API key was read, no Cloudflare Supabase variable was entered, no Auth user was created, and no migration was applied.
- Status is **`STAGING BLOCKED`**, not accepted: Basic Auth and an isolated Staging project exist, but Backup/Restore evidence and the authorized Staging migration gate have not occurred, the application is not configured with the Staging project, and GitHub Workers Builds has not been connected in the Cloudflare dashboard. These conditions prevent live auth/booking/payment/storage/email/monitoring verification and correctly leave every write journey unverified.
- Evidence: `docs/CLOUDFLARE_STAGING_DEPLOYMENT_EVIDENCE_2026-08-26.md`. Exact next task is the single owner-side Cloudflare/Supabase Staging configuration described there; afterwards, perform only the authorized Staging recovery, migration and acceptance gates.

## Supabase Staging recovery and migration gate — 2026-08-26
- Production `zfbw…jidc` and the separate empty Staging project `uecv…cphp` are both active; no Production connection, API key, migration, Auth record, Storage object or Production write was accessed in this gate.
- `RESTORE DRILL BLOCKED`: official PostgreSQL tools are available, but the executor has no `HEBA_LAUNCH_PRODUCTION_DATABASE_URL`. The runner was tested fail-closed and created no backup artifact or restore target when the input was absent.
- Local migration evidence passed: 48 sequential source migrations (`000`–`047`) have no duplicate/missing prefixes, `audit:db` passed, and the booking contract verifier confirmed `044 → 045 → 046 → 047`. Baseline, parity, Staging writes, pending migrations, synthetic tests, Auth and Admin remain unverified and unapplied.
- Recovery tooling now derives the organization/region from the authorized healthy Production ref and cleans its temporary backup artifacts after a drill. Evidence and the exact unblock condition are in `docs/SUPABASE_STAGING_MIGRATION_AND_ACCEPTANCE_2026-08-26.md`.

## Production launch closure checkpoint — 2026-08-26
- Current decision is **`BLOCKED — CLOUDFLARE OWNER SETUP AND STAGING ACCEPTANCE REQUIRED`**. A fresh read-only CLI check confirms production ref `zfbwpubsnuijybxjuidc` is active and migration history remains 000–043; 044–047 are pending. The provider reports PITR disabled and no physical backup, and no staging branch exists.
- The public endpoint responds over HTTPS but has not been proven to run this source release under the approved Cloudflare architecture. No Cloudflare account, Worker, DNS or route has been inspected or changed.
- The governing closure/owner-input record is `docs/PRODUCTION_LAUNCH_CLOSURE_2026-08-26.md`. All local-ready work remains valid, but Cloudflare owner setup, recovery, staging, approved legal/commercial data, payment/email/monitoring, and hosting control must close before production writes or customer access.
- The authorized logical-backup fallback was attempted safely but did not begin a dump because Docker Desktop (the CLI Postgres dump runner) is unavailable. No production data was exported; the resulting zero-byte ignored placeholder is not backup evidence.
- Final local release gate passed: `pnpm check:deploy`; public Playwright **46/46** with no skips; `pnpm package:release` (411-file recursive inspection); then `pnpm audit:security`. This proves only local release hygiene, not staging acceptance or production readiness.

## Cloudflare Workers compatibility spike — 2026-08-26
- The active architecture is GitHub Free → Cloudflare Workers Free/vinext with Cloudflare DNS/CDN, separate Supabase Free Production/Staging projects, Resend Free, Sentry Developer Free, logical backup/restore drill and governed manual payments. Namecheap is registrar-only; the prior Vercel and cPanel routes are retired.
- The safe source branch `codex/cloudflare-compatibility-spike` is pushed to the owner-selected GitHub repository. GitHub OAuth and Cloudflare OAuth are verified locally; no Worker deployment, Workers Builds connection, DNS change, secret entry, Supabase change or Production action occurred. The repository visibility is owner-approved Public, so no secret may ever be committed.
- Branch `codex/cloudflare-compatibility-spike` contains the non-destructive vinext/Vite/Wrangler setup. The official `vinext check` found no active application import/configuration incompatibility; its 90% headline included retired CommonJS and ignored artifacts, not an active runtime dependency.
- The new isolated Workers builder excludes every `.env*` path and passed; Wrangler local smoke returned 200 for `/`, `/robots.txt` and the image route, a protected 307 for anonymous admin, and enforced CSP/HSTS/nosniff from the Worker proxy. No external configuration or secret was used.
- The direct Supabase signed-upload/finalize path now covers protected large delivery, payment proofs and administrative media. Browser uploads never pass their file bodies through the Worker; the final server action repeats authorization, validates metadata, persists the index, records audit data and cleans failed uploads. This is a local P0 hosting-compatibility closure, not live Storage evidence.
- `pnpm test:e2e:cloudflare` passed **46/46** against the locally generated Worker runtime after the direct-upload conversion. `pnpm check:deploy` then passed: isolated credential-free type/build, the ordinary 60-route Next build, **46/46** public desktop/mobile E2E, and all archive/delivery/booking/security/admin/media/commerce/catalog/database/launch audits. This is local parity evidence only.
- Final local packaging passed with 418 inspected files and no local secrets/build/test artifacts; `audit:security` and `audit:launch` also passed. The source archive is a local hand-off artifact, not a deployment authorization.
- Next task: the single `OWNER CLOUDFLARE SETUP — ONE CONSOLIDATED REQUEST`; after it is complete, perform the recovery/Staging/provider gates without re-evaluating the project.

## Production launch continuation — 2026-08-26
- Owner authorization is received for disposable staging, logical backup/isolated restore, reviewed manual payment, canonical `https://hebaelsherif.com`, and production rollout only after staging acceptance and legal/content approval.
- Supabase preview branching was attempted safely without `--with-data`, but the provider rejected it with `402 entitlement_required` because this organization is not on Pro. No paid upgrade was made. A free separate staging project was successfully proven available, then deleted while still entirely empty because its transient generated database password was not retained; no Production or customer data was ever copied.
- Official portable PostgreSQL 17.11 tools now exist only in ignored, ACL-restricted launch tooling. The syntax-checked backup/restore runner and recovery-drill orchestrator fail closed unless a secure executor-only Production database connection is supplied; the latter creates and deletes the isolated restore project internally. The executor has no such production connection source configured, so no logical backup, restore, migration, or staging data setup has occurred.
- Cloudflare OAuth is authenticated, but no Worker capability has been modified and no email/monitoring configuration name is present. The current owner action is the single consolidated Cloudflare setup request in the launch closure, including the dashboard-only GitHub Workers Builds connection; all other local preparation continues without publishing drafts.
- Release packaging now explicitly excludes `.launch-backups` and `.launch-tools`; the final updated package passed at 411 files, followed by `audit:security` and `audit:launch` passes.
- The complete `pnpm check:deploy` gate was rerun after the exclusion correction and passed: isolated build (60 routes), **46/46** public Playwright with no skips, and every route/UX/color/security/admin/media/commerce/catalog/booking/database/launch audit. This remains local evidence only.

## Superseded Namecheap Hosting slice — 2026-08-26
- Historical only. This work was retired before the Cloudflare spike: there is no Namecheap Hosting subscription, no cPanel account was inspected, and no such configuration is active. The final architecture is the Cloudflare Workers path above; Namecheap remains the domain registrar only. The previous decision history remains preserved in `docs/DECISIONS.md`.

## Road to 100 remediation — Gate 0 / local-safe slice — 2026-08-25
- The governing acceptance score remains **54/100** and the launch label remains **`BLOCKED — VERIFICATION REQUIRED`**. No staging/production/provider/Auth/hosting/payment/backup action, secret read, or migration was performed in this slice.
- Local migration order is now explicitly verified as **044 → 045 → 046 → 047**. `045` is the forward-only booking least-privilege companion to 044; a source verifier plus sanitized-metadata preflight/postflight validator and fixtures are available. The validator requires a separate staging project, a named verified recovery/restore drill, strict postflight grants/RLS/tables, and removal of the direct pending-booking policy.
- `docs/STAGING_BOOKING_CHANGE_RUNBOOK_2026-08-25.md` contains the owner authorization boundary, dry-run evidence contract, exact postflight conditions, and a forward-only rollback decision tree. Gate 1 is **awaiting-owner** for staging recovery/PITR and restore evidence.
- Draft legal fallback routes are removed from the sitemap and emitted `noindex,nofollow` metadata. Android/iPhone resilience now explicitly requests resize-on-keyboard content, prevents unexpected mobile text inflation, avoids fixed-background jank on phones, and has local 390/768/1440/reduced-motion regression coverage. These are local improvements, not legal approval, staging, or live performance evidence.
- Final local verification for this slice passed: `pnpm check:deploy` includes the new staging-contract fixtures and completed with 46/46 public Playwright tests (no skips); `pnpm package:release` created a recursively inspected 406-file archive and a final `pnpm audit:security` passed. The external launch classification is unchanged.

## Full-stack review checkpoint — 2026-08-25
- Current objective: close locally verifiable gaps from the owner-requested fourteen-layer review without external writes, then retain the accurate release label.
- Completed local security slice: a **fresh** TOTP/WebAuthn AMR event no older than ten minutes is now required server-side for payment approval/rejection, refunds, role grants/revokes/permission edits, and operational payment settings. `/auth/admin/mfa?reauth=1` now requires a new code even when the session is already AAL2; it no longer auto-redirects. `verify:fresh-admin-assurance` and the expanded admin audit pass locally.
- Completed local operations-truth slice: reports now return `ready`, `unconfigured`, or `error`; the protected reports page does not render unavailable source data as zero KPIs, and snapshot creation rejects a non-ready source. This closes the local misleading-zero path while provider/staging evidence remains separate.
- Governing review ledger: `docs/FULL_STACK_REVIEW_AND_UPGRADE_2026-08-25.md`. Exact next task: run the complete isolated local release gate, package/archive inspection, then record outcomes; staging/production remain blocked pending owner authorization and recovery/Auth prerequisites.

## Master execution checkpoint
- Source directive: `docs/DEVELOPMENT_ROADMAP_2026-08.md` supplements the owner-approved master execution prompt. Security reset precedes reports/design work.
- Current objective: continue product completion locally from a route-by-route audit; remove ungrounded public promises and dead purchase/contact paths, make content readiness measurable in the protected admin, and leave Supabase, migrations, keys, payments, and production untouched.
- Master launch local completion (2026-08-20): added forward-only local migrations 045 (booking least privilege), 046 (media rights/caption/credit/folder/focal governance), and 047 (legal approval/version/effective date). Booking local verification now covers hold expiry/release, duplicate denial and Cairo DST; a dedicated role verifier proves anonymous discovery-only access and revocation of legacy/direct booking bypasses. CMS creation/sections now use a fixed component registry and server-side link/content validation; legal and FAQ routes consume only published governed content and retain truthful fallbacks.
- Release isolation correction (2026-08-20): the first full gate exposed that a normal Next build could bake ambient local public configuration. `build:isolated` now explicitly shadows public and server Supabase variable names with empty values without reading any credential file. The isolated build and 39-test public desktop/mobile suite pass; one desktop-only geometry case is intentionally skipped. `.env.example` was also corrected to contain names only after a credential-shaped value was detected, and `audit:security` passes.
- Readiness/operations completion (2026-08-20): Admin overview includes reschedule, system-alert and launch-blocker signals plus real quick-action routes. `/admin/system` maps every check to `ready`, `warning` or `blocker` and refuses Launch Ready while any blocker remains. The authoritative local ledger is `docs/MASTER_LAUNCH_EXECUTION_2026-08-20.md`; current classification is `LOCAL READY — EXTERNAL SETUP REQUIRED`.
- Master launch release gate (2026-08-20): `pnpm check:deploy` passed after isolated type-check, lint, archive/delivery/booking/CMS checks, production build, public desktop/mobile Playwright (39 passed, one intentional desktop-only skip), and all route/UX/color/security/admin/media/commerce/catalog/booking/database/launch audits. `pnpm package:release` then produced a recursively inspected 397-file archive and the final security audit passed. File count is evidence, not a target; no screenshot/test/build artifact or local secret is packaged.
- Full upgrade Phase A–C continuation (2026-08-20, local only): added `docs/FULL_UPGRADE_EXECUTION.md`, `BOOKING_LAUNCH_RUNBOOK.md`, `CONTENT_OPERATIONS_GUIDE.md`, and `RELEASE_EVIDENCE.md`. Migration 044 now contains a non-sensitive runtime-contract RPC, and booking data checks that contract before treating availability as readable. Missing 044 no longer degrades silently to an apparently normal empty catalog: the public surface stays safe while `/admin/system` reports a specific booking-contract blocker. Paid and package booking source paths now obtain a hold before invoking hold-aware RPCs; direct browser execution of the older non-hold booking RPC is revoked. Local booking contract, booking audit, TypeScript, and targeted lint passed. No configuration, key, Supabase project, or migration was touched.
- Full upgrade Phase A release correction (2026-08-20, local): the first audit caught that a stale release staging tree still contained `.pnpm-store` and `debug.log`. The packager and audit now explicitly exclude both, alongside existing archive/dump/secret/test exclusions. `verify:archive-security` passed; after regeneration `package:release` produced a recursively inspected 387-file archive and final `audit:security` passed. The intermediate failed audit is retained as evidence of the fix; the count is not a release target.
- Full upgrade Phase C hold UX (2026-08-20, local): the customer journey now acquires a customer-owned hold after intake and shows a real countdown in review; navigating back or changing service releases it. Free, paid, and package confirmation consume that existing hold rather than silently creating a second hold. A customer-owned, no-store ICS route is available for pending/confirmed appointments. The source test and audit still do not substitute for a database/RLS/provider/browser scenario against an authorized project.
- Full upgrade Phase D–E local slice (2026-08-20): the public home now includes the existing truthful decision journey and editorial feature rather than leaving those completed components disconnected. Admin overview no longer renders unconfigured or failed operating queries as zero KPIs; it links to system health until a read succeeds. TypeScript and targeted lint passed. Real content, CMS mutation, owner/admin workflow, and live query verification remain distinct follow-up evidence.
- Full upgrade Phase F–H local evidence (2026-08-20): local booking and protected-delivery contracts, recursive archive security, type-check, full lint, blank-public-config production build, and all eleven source audits passed. Public Playwright passed 39 tests with one intentional desktop-only skip. Visual QA at 1440px and 390px found the hero entrance initially hid copy; it was corrected to a visible 400ms transform-only motion and rechecked at both sizes. `package:release` then passed with a recursively inspected 388-file archive and final security audit. No live verification, secrets, project connection, migration, payment, or legal/provider claim was made.
- Live read-only preflight (2026-08-20): the owner-confirmed production ref is `zfbwpubsnuijybxjuidc` (HebaElSherif, `ACTIVE_HEALTHY`). Remote migration history is contiguous through 043; 043’s protected-delivery tables, constraints, indexes, RLS, service-only RPC ACLs, and private delivery buckets were verified from the live catalog. Migration 044 and all of its new tables, columns, RLS, and RPCs are absent. This is not deployment approval: a live anonymous grant on legacy `create_booking_order`, the authenticated direct pending-booking policy, implicit internal-helper grants in 044, no available backup/PITR recovery point, and unverified Auth redirects block staging. Full evidence and rollback conditions: `docs/LIVE_READONLY_PREFLIGHT_2026-08-20.md`. No data, Storage object, payment, booking, migration, or secret was accessed or changed.
- Product Completion Phase 2 — booking operational slice (2026-08-19, local only): added forward-only `044_booking_operational_workflow_local_only.sql`; it is not applied anywhere. It introduces service-specific payment/notice/buffer/window/hold/cancel/reschedule policy, public time-only availability calendar RPCs, manual slot opens/closes, holidays/blackouts, short-lived customer-owned holds, free booking confirmation from a hold, customer reschedule validation, admin reschedule resolution, and validated admin booking updates. The public wizard now uses database-derived available slots, has an explicit review step, and permits a service explicitly marked `free` to complete without configured payment. Admin has service policy fields, exception/slot controls, and a day/week/month searchable agenda with request resolution and internal notes; the customer dashboard has real status, requests, and event timeline. No environment was contacted and no claim of a live migrated end-to-end workflow is made.
- Product Completion Phase 2 local contract verification (2026-08-19): `node scripts/verify-booking-local.mjs` passed an isolated Admin→availability→customer→hold→free confirmation→cancellation/reschedule scenario, including concurrent same-slot holds, buffer, minimum notice, blackout, and manual open/close denial/acceptance. `audit:booking`, TypeScript, and targeted lint passed. This is a source-level contract test, not a Supabase integration test or browser E2E against a configured project.
- Product Completion Phase 2 local verification continuation (2026-08-19): full ESLint, blank-public-config production build, all eleven source audits, and `package:release` passed. The recreated release archive contains 386 files after recursive inspection; that number is evidence only, not a target. The isolated public Playwright suite passed 39 tests with one intentional desktop-only skip. Actual local screenshots at desktop and 390px show the truthful `/booking` empty state and the protected mobile admin entry; a configured booking/admin journey cannot be rendered without the prohibited Supabase connection.
- Product Completion Phase 1 (2026-08-19, local): created `docs/PRODUCT_COMPLETION_AUDIT.md` for every expected public, customer, admin, and checkout route. It records true source, operational state, admin control, and test gap. The audit does not turn a missing database, policy, or content row into a ready state.
- Product Completion Phase 1 public corrections (2026-08-19, local): FAQ payment copy is derived from actual configured methods; contact renders a no-send state when public Supabase configuration is absent; product and workshop details do not render purchase controls without an enabled payment method; booking excludes services that lack published availability rules instead of fabricating weekday/time windows. Static named products, time-bound promises, claimed recordings, discount urgency, and unsupported response-time claims were removed or replaced by data-bound copy. Legal pages now explicitly identify unapproved terms, refund, and privacy texts as non-production drafts rather than presenting unconfirmed conditions as binding policy.
- Product Completion content readiness (2026-08-19, local): `/admin/system` now has a read-only readiness checklist for public configuration, published products, services plus availability, enabled payment methods, articles, and legal approval. With no configured public source it reports `غير مهيأ`; it never treats fallback copy as ready and does not expose payment values or credentials.
- Product Completion verification (2026-08-19, local): direct TypeScript and ESLint checks passed; a production build with all public Supabase variables explicitly blank passed. The isolated public Playwright suite passed 40 tests with one intentional desktop-only skip, including truthful no-payment/no-contact assertions and fresh visual captures at 1440px and 390px CSS widths. `verify:archive-security`, `verify:delivery-local`, and all eleven local audit scripts passed. `node scripts/package-release.mjs` then generated a recursively inspected 383-file archive and the final `audit:security` passed. The count is evidence only, not a release target. The `pnpm` wrapper was not used because its known environment failure occurs before script execution.
- Completed in Phase 0: repository/release hygiene, security audit hardening, continuation documentation, and clean archive verification.
- Completed in Phase 1: migrations 019–020 applied live; eight roles and 118 mappings; database `has_permission()`; centralized `requirePermission()` across privileged actions; permission-aware RLS; route boundaries and filtered navigation; owner-only grant/revoke with last-owner protection; shared admin table/editor primitives; live owner/support verification; desktop/mobile browser QA; full `check:deploy` and 294-file clean package passed.
- Completed in Phase 2: migration 021 applied live; metadata-rich public/private library; MIME/size validation; search/filter/pagination; metadata editing and URL copy; usage registry plus legacy-reference deletion guard; picker integrated across product/course/book/workshop/article covers; disposable live visibility/deletion verification; desktop/mobile QA; full gate and clean package passed.
- Completed in Phase 3: migrations 022–024 applied live; atomic/idempotent checkout, proof submission, payment approval/rejection, order cancellation/expiry/refund, bundle/domain access grants and refund reconciliation; RPC privilege isolation; live concurrent retry tests; admin review browser QA; full gate and clean package passed.
- Completed in Phase 4: variant CRUD and active states; bundle composition with nested-bundle prevention; variant-aware atomic checkout with server-authoritative pricing; typed validated payment/expiry settings replacing raw JSON for critical operations; checkout consumes enabled methods; live bundle/variant grant and refund verification; responsive settings QA; full gate passed.
- Completed in Phase 5: migrations 026–030 live; serialized multi-window availability; typed booking policy; immutable fingerprinted/scoped credit ledger; plan eligibility, snapshots and capacity; atomic activation/opening grants; package checkout; exactly-once cancellation restoration; customer cancel/reschedule; admin agenda; responsive management UI; live concurrency verification; production build, all audits, and Playwright 31 passed/3 skipped.
- P0 closure before Phase 6: migrations 031–033 live; permission privacy/fail-closed authorization and owner permission editor; Git-independent secret audit and `pnpm deliver`; configured payment-method enforcement; coupon scope/stacking recheck; entitlement/refund ledgers; free fulfillment; multiple-purchase-safe revocation; atomic workshop last-seat/release; private workshop meeting links; paid package fulfillment. Live commerce integrity verification passed.
- Completed in Phase 6: fixed workshop detail route; signed owner-gated book downloads with logs; registered-only workshop meeting/resources/recordings; direct signed large-file uploads; book editions; course video/resource delivery; editable/reorderable/deletable curriculum; atomic workshop registration/attendance operations; migrations 034–035 live; delivery verification, production build, audits, browser admin check, and Playwright 31 passed/3 skipped.
- Completed in Phase 7: migration 036 live; structured pages/sections; revision snapshots; secure 30-minute previews; scheduled publishing with cron; article scheduling; canonical/OG controls; fully editable header/footer navigation consumed publicly; owner-editable About profile without invented claims; approved matte palette aliases; CMS verification, production build, browser QA, and Playwright 31 passed/3 skipped.
- Completed in Phase 8: migrations 037–038 live; customer 360 notes/tags/timeline; customer-linked collaborative inbox with assignment, priority, spam, notes and retained disabled outbox; tokenized unsubscribe; purchase-verified review RPC with consented display name, moderation/reject/archive/restore, owner response, audit history, and direct-write denial. Live `verify:crm`, type-check, and lint passed.
- Security reset evidence (2026-08-16): release/package audits are clean; current source is prepared to prefer `SUPABASE_SECRET_KEY`; legacy elevated-key compatibility remains only for a controlled cutover. Supabase reports physical-backup support enabled but no listed recovery point/PITR; browser automation is unavailable in this environment, so dashboard-only rotation, account MFA, session revocation, and restore rehearsal remain external operations.
- Cycle 1 security slice (2026-08-16): migrations 039–042 are live. The password-first admin flow now gates on a server-held role lookup, `/auth/admin/mfa` enrolls/challenges two TOTP factors, proxy and server authorization require AAL2, and shared RLS helper functions deny privileged AAL1 sessions while preserving explicit service-role workflows. Privacy-minimized admin authentication events are recorded without tokens, raw IPs, or user agents.
- Cycle 1 session controls (2026-08-16): administrative access additionally requires an opaque, HttpOnly application session with a 30-minute idle limit and eight-hour absolute limit. The security page lists active application-admin sessions and can revoke one or all; only hashes/fingerprints and a coarse derived browser label are stored. A server-only serialized throttle blocks after five failed attempts for five minutes, escalating to 30 minutes after ten attempts.
- Runtime reliability slice (2026-08-16): development CSP now permits `unsafe-eval` only while React development tooling is active, while production continues to prohibit it. This restored React hydration and client controls. A production build then exposed and corrected an invalid client import of server-only MFA-session code. Desktop E2E passed completely; mobile E2E passed 15 tests with 2 intentional authenticated skips; dark-theme persistence and the five-step booking UI are covered.
- Cycle 2 protected-delivery slice (2026-08-18, local only): additive migration 043 adds atomic five-downloads-per-book/24h admission, privacy-minimized delivery events, a 15-minute video admission session, one concurrent stream, a two-device/30-day ceiling, and service-only RPCs. Books/resources/recordings use no-store/no-referrer redirects and 60–120 second signed URLs; lesson video and resource actions return application routes rather than Storage tokens. Admin uploads use opaque UUID object names, exact extension/MIME allowlists, server-observed byte size and MIME, magic-byte checks, and hashed-path inspection records. If an external scanner is configured, unavailable/non-clean verdicts block binding; when no scanner is configured, binding proceeds after local signature/MIME/size validation, so this mode is not antivirus fail-closed and makes no malware-scan claim. Production build, type-check, targeted lint, route/database/media/security audits passed locally.
- Release archive hardening (2026-08-18, local): source archives and database dumps are excluded from staging without deleting the owner's originals. The shared archive gate identifies ZIP/TAR/TGZ by content as well as name, recursively lists and extracts supported nested archives into bounded temporary directories, rejects unsafe paths, `.env`, `supabase/.temp`, `.git`, dumps/backups, private keys, test/private artifacts, symlinks, unsupported archives, and secret-like content. `audit:security` demonstrated an explicit failure against the prior nested `hebaelsherif.zip`; the regenerated source archive contains 371 files, no nested archive, and passed recursive content inspection, member-for-member manifest comparison, the complete local `check:deploy`, and the final `audit:security`. The owner's original ZIP remains untouched. File count is recorded as evidence only, not a target.
- Delivery-test coverage (2026-08-18): `verify:delivery-local` executes the same validation function used by finalization and proves valid PDF magic acceptance, disguised executable rejection, MIME mismatch rejection, validation-before-bind ordering, and schemas/audit metadata without raw token or storage-path fields. The live `verify:delivery` script is extended—but deliberately not run—to prove entitlement denial after revocation, actual Storage range inspection for accepted/rejected magic bytes, atomic download limits, session/device limits, and persisted delivery/inspection logs without raw token/path values once the production ref is confirmed. No live success is claimed.
- Cycle 4 brand/public journey (2026-08-18, local only): `/` now uses a slim cream RTL header with real navigation, persisted light/dark control, and one dark login action; a desktop three-part hero places the existing still-life at the left, factual Arabic copy at the center, and an original generated portrait of a fictional adult Arab niqabi reader at the right. The image shows only the eyes, is explicitly captioned as representative, is not presented as Heba ElSherif, and is self-hosted as the 230 KB optimized asset `public/brand/hero-reader-niqab-v1.jpg` (the untouched generated PNG source remains outside the release workspace). At 390px the document order is copy, actions, then portrait, with the eyes retained in-frame and no horizontal overflow.
- Cycle 4 content truth (2026-08-18): the homepage value rail and four path cards use neutral, verifiable descriptions for books, courses, sessions, and workshops. Synthetic offer/article/testimonial fallbacks and the newsletter/claim-heavy homepage sections were removed from `/`; offers, articles, and reviews render only when active/published/approved rows actually exist. Payment, authorization, booking internals, Supabase, and migrations were not changed.
- Cycle 4 public-truth and guarded-state completion (2026-08-18, local): catalog, booking, checkout, learning, and admin paths no longer manufacture no-environment/error data, payment destinations, learner progress, or administrator access. `/services` now returns an honest empty state when no public client is configured; booking links to the real services route when no session is open. The proxy redirects protected dashboard/admin paths to their authentication routes if configuration is absent or an identity lookup fails, and the server-side admin gate no longer permits a zero-data demonstration mode. The unused public newsletter component was removed; account settings accurately say that periodic email is not currently sent.
- Cycle 4 verification (2026-08-18): actual browser QA passed at 1440×900 and 390×844 in light mode, with a desktop dark-mode toggle exercise, clean browser error/warning logs, semantic navigation/labels, visible focus styles, intact image framing, and zero horizontal overflow. The initial screenshots were generated as excluded test output and are no longer retained in this workspace. `pnpm type-check`, `pnpm lint`, the production build with public Supabase variables explicitly blank, and the complete local `pnpm check:deploy` gate passed at that checkpoint. `pnpm package:release` then produced a recursively inspected 372-file source archive and the final `pnpm audit:security` passed; the count is evidence, not a target.
- Cycle 4 public-truth verification (2026-08-18): with all public Supabase variables blank, direct local requests proved `/booking` and `/services` return 200 with their honest empty states, while `/dashboard` and `/admin/overview` return 307 to the correct auth route. Type-check and lint passed; `verify:delivery-local`, `verify:archive-security`, and all eleven local audits passed. Public Playwright passed 33 tests with one intentional desktop-only skip, covering desktop/mobile public routes, dark-mode persistence, truthful booking availability, auth chrome isolation, protected-route redirects, a visible keyboard skip link that moves focus to the main content, and mobile overflow. The production build now passes without network font access: self-hosted Arabic subsets of Amiri and IBM Plex Sans Arabic replace the four `next/font/google` imports, preserve the existing heading/body system, and prevent third-party font fetches at build or first paint. The final recursively inspected release package is recreated after this record; its file count is evidence only.
- Deployment-gate refinement (2026-08-18, local): `test:e2e:public` is a dedicated, configuration-isolated command for the non-destructive public browser suite and is now part of `check:deploy` after the production build. Its web server explicitly blanks public Supabase variables, so this gate cannot use an ambient project. The separate authenticated suite deliberately remains outside this gate because it creates and deletes controlled users through a service role and requires a confirmed disposable target.
- Tooling note (2026-08-18): the local `pnpm` wrapper currently fails with `fetch failed` before it runs a script, including `pnpm test:e2e:public`. This environment fault prevents claiming a literal `pnpm check:deploy` run. The underlying local executables completed the same safe gate components: type-check, lint, production build, archive/delivery verification, all audits, and the public browser suite. No dependency was installed or updated to mask the failure.
- Owner decision blocker (only external blocker; 2026-08-18): confirm which Supabase project ref is authoritative production—`azuvwkzpgtyxwxmvedmp` or `zfbwpubsnuijybxjuidc`. Migration 043 is not live, no migration was pushed, and no new key is requested at this checkpoint.
- Exact next product task: use the admin content-readiness checklist to publish owner-approved real content and legal policies, then exercise the corresponding authenticated workflows only against the owner-confirmed project. The separate delivery preflight remains: after the owner confirms the Supabase ref, perform a read-only identity/history/schema check before any live mutation and run deferred live delivery verification only when explicitly authorized.
- Existing product baseline: V2.7 implementation and live migrations 000–018 are preserved. The historical status below is evidence, not a substitute for the new phase gates.

## Master phase ledger
- [x] Phase 0 — baseline, repository safety, release hygiene, continuation docs
- [x] Phase 1 — admin foundations and granular permission enforcement
- [x] Phase 2 — complete media library and reusable picker
- [x] Phase 3 — atomic commerce and payment approval/rejection
- [x] Phase 4 — catalog and owner-managed platform settings
- [x] Phase 5 — booking windows, capacity, packages, and credit ledger
- [x] Phase 6 — courses, books, workshops, and delivery controls
- [x] Phase 7 — CMS, menus, SEO, and brand controls
- [x] Phase 8 — CRM, communications, testimonials, and reviews
- [ ] Phase 9 — reports, exports, offers, and marketing operations
- [ ] Phase 10 — hardening, accessibility, performance, and launch rehearsal

## Historical implementation record
## Completed phases
- V0.1.0 ✅ foundation: Next.js 16 + TS + Tailwind v4, RTL root, Arabic fonts, brand tokens, audit scripts, docs, check:deploy green
- V0.2.0 ✅ brand system: 10 ui primitives (src/components/ui/ + index barrel), BrandLogo SVG substitute, PublicHeader/PublicFooter, DashboardShell/AdminShell, (public) route group wired
- V0.3.0 ✅ Supabase: migrations 001–010 (all §6 domains + RLS + storage), auth pages, middleware guards, guarded seed, SUPABASE_SETUP.md
- V0.4.0 ✅ homepage: hero (split, portrait+floral SVG substitutes), trust strip, 4 service cards, live offer countdown, featured articles, testimonials carousel, newsletter form — Supabase-backed with editorial fallbacks; visually verified in browser
- V0.5.0 ✅ discovery: /courses (S2 full: category strip w/ counts, cards w/ badge+rating+lessons, comparison panel, offer countdown, testimonials, CTA ribbon), /books, /workshops, /services, /booking, detail pages (course curriculum accordion, book cover art, workshop countdown+seats), /articles + [slug], editorial (about/start-here/faq/contact form→contact_messages), legal ×4 via shared ProsePage, custom 404 — 22 public routes in manifest
- V0.6.0 ✅ checkout: stepper (summary/coupon/method → instructions/proof upload → confirmation), server actions with server-trusted pricing + service-client coupon validation, payment-proofs upload, awaiting_review transition, audit logs, /dashboard/payments timeline
- V0.7.0 ✅ customer dashboard: 10 routes (home w/ greeting+continue-learning+upcoming session+stats, courses w/ progress bars, books, workshops, bookings, orders table, payments timeline, notifications w/ mark-read, profile form, settings w/ password change) — all with real-data queries + polished empty states
- V0.8.0 ✅ LMS player: /dashboard/courses/[slug]/learn — video shell w/ signed-URL playback (enrollment/preview-gated), curriculum accordion w/ per-module x/y + checkmarks, mark-complete w/ percent recompute, prev/next, notes CRUD, resources signed downloads, progress ring, not-enrolled state, demo mode
- V0.9.0 ✅ admin foundation: requireAdmin layout gate + sidebar badges, /admin/overview (KPI cards w/ sparkline, 6-month revenue SVG chart, bookings donut, approvals preview, recent customers, today schedule), /admin/payments functional queue (proof signed-URL preview, approve → paid + access grants by product type + enrollment/book_access/registration + notify + audit; reject → required reason + order back to pending_payment + notify + audit)
- V1.0.0 ✅ MVP gate: README, frozen-lockfile verified, full check:deploy green, tagged v1.0.0
- V1.1.0 ✅ commerce completion: expire_stale_orders() (migration 011 + pg_cron notes), bundle child-grant expansion, /admin/orders lifecycle (status filters, cancel/refund w/ notify+audit, refund revokes content_access), inline proof re-upload for rejected payments
- V1.2.0 ✅ offers/coupons: /admin/coupons + /admin/offers CRUD w/ activate toggles + redemption counts, coupon redemption recorded at approval, offer-aware server-side pricing (resolveActiveOffer/applyOffer in checkout page AND createOrder), offer label at checkout. Note: discovery-card offer badges still via compareAtPrice; live offer badges on cards → V1.6.0 polish
- V1.3.0 ✅ reports: /admin/reports (revenue by month + by product type, per-course enrollments/avg-progress/completions, bookings by status), report_snapshots save+list, track() analytics helper wired into checkout (order_created, proof_submitted) + newsletter (subscribed)
- V1.4.0 ✅ CMS + full admin coverage: all 23 §5 admin routes live — products/books/courses/workshops publish toggles, curriculum builder (add module/lesson), bookings status controls w/ notify, users list, articles create+publish, reviews approve/feature/delete, pages SEO editor, media registry, settings editor + feature-flag toggles (flags drive public nav), roles grant/revoke (owner-only), audit-logs viewer, security posture page, system status
- V1.5.0 ✅ security hardening: rate limits (coupon 10/5min, proof 5/10min), migration 012 (payments amount = order total, orders total consistency, notification-update trigger guard, anon payload size caps), docs/SECURITY_REPORT.md
- V1.6.0 ✅ visual polish: browser pass done — **fixed demo-mode 500 on all guarded routes (middleware now passes through without env)**, Arabic lesson pluralization (lessonsLabel), Arabic-Indic countdown digits, S4 streak day-dots + achievements feed on /dashboard. (Historical note: reference images were supplied later and incorporated in V2.4/V2.5.)
- V1.7.0 ✅ mobile QA: responsive sidebar (mobile top bar + RTL slide-over drawer, verified interactively), shells flex-col→lg:flex-row, programmatic overflow sweep 375/768/1024 across 26 routes — zero overflow, MOBILE_QA_REPORT.md
## Current phase tasks (V1.8.0 staging)
- [x] **LIVE SUPABASE CONNECTED** — user provided .env (project azuvwkzpgtyxwxmvedmp "HebaElSherif", eu-central-1); fixed URL (was dashboard URL → API URL)
- [x] Found pre-existing legacy schema w/ REAL business data (2 sessions 1200/1500 ج.م, Instapay 01037141322, brand tagline) → migration 000 relocates legacy tables non-destructively to `legacy` schema; 013 ports the real data into our schema; owner role auto-grants to heba0elsherif@gmail.com on signup
- [x] All 14 migrations pushed via supabase CLI (repaired legacy history 0001–0009 as reverted); buckets + flags + settings verified live
- [x] Live-mode semantics: empty catalog → honest empty states (fallbacks now only for no-env/error), unknown checkout product → 404, unconfigured payment methods hidden (bank hidden — only instapay+wallet configured), hero fabricated stats replaced with honest descriptors, booking hides empty availability
- [x] Live RLS validated with temp user (profile trigger ✓, valid order ✓, tampered total rejected 42501 ✓, coupons invisible ✓, cleaned up)
- [x] Restored .env.example (was renamed to .env by user; .gitignore now !.env.example)
- [x] DEPLOYMENT.md + hosting runbook + SUPABASE_SETUP.md refresh; pg_cron scheduled live (migration 014)
- V1.8.0 ✅ staging: live Supabase integrated, legacy preserved+ported, deployment docs, expiry cron live
- V1.9.0 ✅ production hardening: error.tsx + global-error.tsx boundaries, loading.tsx ×3 groups (PageSpinner), robots.ts + sitemap.ts (dynamic slugs), metadataBase + OpenGraph/Twitter meta, middleware→proxy rename (Next 16 deprecation gone)
- V2.0.0 ✅ FINAL RELEASE: all §16 docs complete (TEST/ROUTE_QA/ADMIN_GUIDE/CUSTOMER_GUIDE/VISUAL_SYSTEM/SUPABASE_MIGRATIONS/FINAL_DELIVERY_REPORT), §15 acceptance verified (frozen-lockfile ✓, check:deploy ✓, no package-lock ✓), tagged v2.0.0
## Current phase tasks
- V2.1.0 ✅ mobile UX pass (user-requested): bottom tab bar, sticky buy bar, safe areas + manifest + icon, touch targets, orders cards on phones — verified live at 375px with real login
- V2.2.0 ✅ brand + luxury + admin elevation (user-requested): official «بذرة نقية» logo as SVG everywhere + app icon, magic-link login (/auth/confirm) + password eye toggle, AdminTopbar global search + quick links + users search + notify tool + pending totals, scroll-reveal/hero entrance/shimmer/gold accents motion system — verified in browser
- V2.3.0 ✅ experience upgrade (user-requested, benchmarked against hebaelsherif.com): true persistent dark mode across public/customer/admin surfaces; password-only customer login and dedicated password-only admin portal with role verification; five-step booking wizard (service/date/time/contact/payment proof), Cairo calendar, occupied-slot filtering, database overlap guard, order-expiry slot release; interactive start-here quiz; searchable/sortable book library — check:deploy green and browser-verified
- V2.3.1 ✅ human-free brand artwork: removed the placeholder portrait/silhouette from homepage and About, replaced with a premium abstract seed composition (architectural arch, orbital lines, botanical geometry, gold/aqua accents), light/dark compatible and browser-verified
- V2.4.0 ✅ visual direction overhaul from the four supplied references: removed the hero artwork component entirely; rebuilt the public experience around a two-tier editorial header, full-width typographic hero, original botanical edge filigree, warm heritage-paper texture, compact trust rail, refined service/article/offer/testimonial/newsletter sections, ivory/taupe footer; elevated shared cards, stats, topbars, admin/learner canvases and deep-teal sidebars with ornamental treatment — desktop + 390px browser QA and check:deploy green
- V2.5.0 ✅ production completion: migrations 015–017 applied live (GiST booking integrity, atomic booking/order RPC, durable PostgreSQL rate limits); password-only admin entry with server-held identity; admin availability editor; semantic dark surfaces; factual copy/currency audit; richer home journey; distinct workspace icons + learner topbar; original no-person still-life WebP used on home/catalog; Playwright desktop/mobile/auth suite (31 passed, 3 intentional skips); full browser light/dark/mobile QA; check:deploy green
- V2.5.1 ✅ public-page refinement: removed the sparse utility header and oversized hero botanicals, replaced duplicated micro-claims with one floating four-point trust panel, unified About/booking/catalog/editorial page introductions, reduced footer/auth ornament, and verified desktop + 390px layouts without horizontal overflow
- V2.6.0 ✅ live integration + brand warmth: remote Supabase migrations 000–017 verified in sync; publishable-key support added; confirmed owner account provisioned for the password-only admin portal; owner-supplied logo adopted across all shells/metadata; public palette warmed and homepage rebuilt as an editorial text + no-person still-life composition
- V2.7.0 ✅ full admin control center: migration 018 live; domain-aware create/edit/delete for catalog and services; advanced per-day availability and booking editing; memberships/subscriptions with capacity and time windows; homepage copy editor; article revisions; inbox/newsletter lifecycle; real Storage uploads; offer/coupon editing; subscription reporting; live CRUD smoke test and browser-verified owner login
- (implementation complete — remaining items are launch/content owner actions)
## Next 3 actions (exact, concrete)
1. (owner) Complete the single [OWNER CLOUDFLARE SETUP — ONE CONSOLIDATED REQUEST](OWNER_CLOUDFLARE_SETUP_ONE_CONSOLIDATED_REQUEST.md), without sharing any secret in chat.
2. (executor) Run the documented logical backup/isolated restore drill, then create the disposable Supabase Staging project and apply 044 → 047 only after the owner setup is complete.
3. (owner) Approve the content and legal pack; then the controlled Staging and Production gates can proceed in their documented order.
## Blockers / needs user input
- The Compatibility Spike is locally complete. Cloudflare/Supabase/Resend/Sentry account access, DNS nameserver delegation and legal/content approval remain external prerequisites; they are consolidated into one request rather than separate prompts.

## 2026-08-27 — Code X competitor-superiority execution

- **Current phase:** Phase 2 implementation is in progress after Phase 0 source-of-truth and comparison acceptance completed locally.
- **Baseline:** `codex/master-merge-2026-08-27` starts from remote `57820ab`; the renamed primary archive was accepted only after a 413-file tracked-source match. Comparison archives remain reference-only.
- **Implemented slice:** secure Admin CSV report export with fresh AAL2, least privilege, fixed columns, bounded dates/rows, spreadsheet-injection protection, fail-closed audit, and private caching.
- **Implemented slice:** governed nonbinding session-policy route, included in the legal approval gate and excluded from indexing while unapproved.
- **Implemented slice:** typed homepage section registry, structured Admin CRUD, revision/audit integration, safe internal links, completeness gate, public renderer, private preview renderer, final CTA, and legacy-section preservation.
- **Verification completed for the slice:** `pnpm type-check`, `pnpm lint`, `pnpm verify:report-export-local`, `pnpm verify:home-cms-local`, and `pnpm audit:cms-local` pass. Full `check:deploy` and updated visual evidence are the current next gate.
- **External boundaries unchanged:** no public push, Cloudflare deployment, Supabase write, DNS change, Production migration, or secret access was performed.

### Next 3 actions (Code X)

1. Run the full local deployment gate and inspect updated desktop/390px homepage evidence.
2. Implement Phase 3 published-only search and improve the guided-start journey without introducing post-047 schema.
3. Continue catalog/customer/Admin parity work, while recording 044–047 and Staging-dependent evidence as `unverified` until the authorized recovery/schema gates exist.

### Phase 3–4 checkpoint

- **Phase 3 local implementation complete:** published-only Arabic-tolerant search with bounded results and `noindex`; desktop/mobile navigation entry; a structured Admin-governed start journey with revisions, audit, internal-link validation, deterministic results and assistive-technology state. Consent analytics were intentionally not added because that boundary is not approved. Formal 049 storage remains deferred until the Staging migration prerequisite.
- **Phase 4 local core implementation complete:** all four domain queries require both domain and product publication; course filters and Arabic catalog search are real-data driven; approved catalog covers are consumed; old/full/zero-capacity workshops cannot enter checkout; direct checkout denies unpublished/unavailable domain targets.
- **Publication gate:** catalog publish validates text, price/currency, 046 media rights/reference, course curriculum/content, protected book file, future workshop/capacity/place, and service availability/payment consistency. Product/domain publish states synchronize with rollback.
- **Current verification:** targeted type-check/lint and the search/start-here/catalog contract checks pass. Updated full build and E2E are the current phase-boundary gate.
- **Development-first boundary:** migration 048 is now authored locally for governed Contact intake and remains unapplied. Resources/Press/consent-complete Testimonials and formal assessment tables may continue as later additive local migrations, while any provider application still requires 044–047 first and the `STAGING EXTERNAL GATE`.

### Next 3 actions (after Phase 4 local gate)

1. Run full build, public E2E (including keyboard start journey and search), security and catalog audits.
2. Audit Phases 6–9 against existing booking/commerce/dashboard/Admin implementations and close all remaining source-level gaps that do not need provider writes.
3. Prepare recovery/schema/runtime evidence as `unverified`, then stop only at the single external recovery/Staging credential gate.
