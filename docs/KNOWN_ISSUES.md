# KNOWN ISSUES

| # | Severity | Issue | Status |
|---|----------|-------|--------|
| 61 | P0 | Admin booking updates and reschedule decisions called 044 RPCs through Service Role even though those RPCs derived authority from `auth.uid()` and were granted to authenticated sessions; legacy status and availability controls also split the business mutation, notification/event and audit across independent writes. | **Locally closed:** migration 061 introduces explicit-actor, permission-rechecked, locked and atomic booking/reschedule/availability RPCs, retires the broken Admin entry points and revokes browser-direct writes. The Admin UI now preserves customer notes, derives duration, exposes only valid transitions and records optional reschedule notes privately. SQL execution, RLS, concurrency and durable persistence remain `STAGING EXTERNAL GATE`. |
| 60 | P0 | Role controls were rendered to non-owners and then rejected only after submission; grants/revokes/audits and permission delete/insert/audits were split writes, while last-owner protection counted outside a serialization boundary and role reads could degrade to partial identifiers. | **Locally closed:** migration 060 adds owner-only bounded/audited reads, self-change denial, direct-write revocation, locked atomic grant/revoke with concurrent last-owner protection, and atomic registry-validated matrix replacement. The page now fails closed, is session-dynamic rather than build-prerendered, and exposes controls only to the owner. Live concurrency/RLS/persistence remain `STAGING EXTERNAL GATE`. |
| 59 | P1 | Manual Admin notification delivery was a direct insert followed by a separate audit write, copied the title into audit metadata, lacked idempotency, and rendered a send control to customer viewers who did not hold `notifications.send`. | **Locally closed:** migration 059 provides one permission-checked/idempotent delivery-and-audit transaction with bounded content and destination allowlists; the UI uses its real permission, exposes truthful options/feedback and no longer duplicates message content into audit. Persistence/RLS and repeat-submit evidence remain `STAGING EXTERNAL GATE`. |
| 58 | P1 | Sentry previously had variable-name readiness only, and isolated builds did not explicitly neutralize monitoring/provider credentials inherited from the shell. | **Locally closed:** official matching Worker/browser SDKs, a custom Worker wrapper, awaited Next error capture, strict PII scrub and secret-shadowed isolated commands are executable and pass both 70/70 suites. A controlled Staging event plus alert receipt remains `STAGING EXTERNAL GATE`; configuration presence is not live proof. |
| 1 | low | Official logo, original botanical SVG system, and an optimized original no-person catalog/editorial still life are integrated. Personal photography is intentionally not required by the approved direction. | closed |
| 2 | low | Four reference screenshots were supplied and translated into V2.4/V2.5 system rules; desktop/mobile/light/dark comparison pass completed. | closed |
| 3 | medium | Order auto-expiry: scheduled hourly via pg_cron on the live project (migration 014). | closed |
| 4 | low | Next 16 "middleware" deprecation: renamed to src/proxy.ts in V1.9.0. | closed |
| 5 | P0 | Release delivery/security audit must work without Git metadata and expose one `pnpm deliver` command. | closed — archive audit verified without `.git` |
| 6 | P0 | Supabase public-key checks remain duplicated and production permission fallback is not yet fully fail-closed. | closed — unified helpers + DB-authoritative fail-closed gate |
| 7 | P0 | Workshop capacity/release and complete entitlement/refund/package fulfillment invariants require atomic database verification. | closed — migrations 032–033 + live integrity verification |
| 8 | P0 | `check:deploy` does not yet include every live verification plus Playwright/accessibility as one command. | partially closed — the local gate now includes 46/46 public desktop/mobile/tablet/reduced-motion/noindex checks with no skips; controlled authenticated/live verification remains separate by design and cannot be folded into a credential-isolated local command. |
| 9 | P1 | Media lifecycle still needs folders, archive/trash/restore/replace and direct large-file upload. | locally closed with executable evidence — direct signed upload already bypasses Worker buffering; migration 054 and `/admin/media` now add recoverable archive/restore, compatible atomic replacement of usages/direct references, public RLS denial, permission recheck and minimized audit. Live persistence/Storage remains `STAGING EXTERNAL GATE`. |
| 10 | P1 | Structured CMS revisions/preview/scheduling and customer 360 communications remain in Phases 7–8. | closed — migrations 036–038 and the Phase 7/8 implementation are recorded as complete |
| 11 | P1 | Date-range financial reporting/export and accessibility/performance rehearsal remain in Phases 9–10. | partially closed — fixed-dataset date-bounded secure CSV export now passes locally; Staging axe/manual screen-reader/Lighthouse and field performance remain open. |
| 12 | P0 | Legacy elevated Supabase key is still the active local compatibility credential until a newly created `sb_secret` key is installed in protected hosting/local environments, verified after deployment, and the legacy key deleted. | open — dashboard/hosting operation required |
| 13 | P0 | Physical backup support is enabled but the 2026-08-20 authenticated read-only preflight reports no listed recovery point and `pitr_enabled: false`; an isolated restore rehearsal cannot be evidenced yet. | open — create/verify a recovery point or PITR and name the restore owner before any staging/production schema change |
| 14 | P0 | Supabase dashboard account MFA, owner password rotation, and active-session revocation are account-scoped controls unavailable to this environment’s browser automation. | open — owner dashboard operation required |
| 15 | P1 | Fresh-AAL2 confirmation has not yet been added to every high-impact administrative mutation (payment approval/refund, role changes, sensitive export, payment configuration and revision restore). | locally closed — the ten-minute step-up covers payment approval/rejection, refunds, role changes, secure CSV export, operational payment settings and revision restore; controlled Staging session/AMR evidence remains under issues 16/25. |
| 16 | P0 | The new two-factor application-admin journey requires a one-time owner QR enrollment/challenge in a real browser before it can be recorded as end-to-end production evidence. | open — owner device operation required |
| 17 | P0 | A prior release nested `hebaelsherif.zip`, which contained `.env`, `supabase/.temp`, and test artifacts that the outer scan did not inspect. | closed — archives/dumps excluded from staging; shared recursive content gate now scans the generated TGZ and causes `audit:security` to fail on forbidden nested paths/content |
| 18 | P1 | Malware scanning has no configured provider evidence, and entitlement-revocation/Storage-magic/log-secrecy checks require a separately authorized live write verification on the confirmed authoritative project. | open — local magic-byte and log-schema tests pass; live `verify:delivery` coverage is implemented but intentionally not run; no antivirus claim |
| 19 | P1 | A fresh production build in the current restricted execution environment cannot download the four `next/font/google` families (Amiri, Aref Ruqaa, Cairo, IBM Plex Sans Arabic). | closed — required Arabic subsets are self-hosted; production build now passes without font-network access |
| 20 | low | The local `pnpm` wrapper previously exited before project scripts in a restricted environment. | closed — literal `pnpm check:deploy`, `pnpm package:release`, and final `pnpm audit:security` passed in this cycle |
| 21 | P0 | Product readiness is incomplete: there is no configured public source in this local environment, no published catalog/service/payment/article evidence was verified, and Terms/Refund/Privacy are explicitly unapproved drafts. | open — publish owner-approved content and legal text, then verify against the owner-confirmed project; do not open purchase/contact flows beforehand |
| 22 | P0 | Booking operational migration 044 and the Admin→customer scenario are source-level only. The confirmed production ref has now been catalog-reviewed, but no 044 migration, write test, payment, booking, or browser journey has been authorized. | open — resolve issue 24, then authorize an isolated staging workflow before any live verification |
| 23 | P0 | The new full-upgrade booking contract depends on 044’s runtime, hold, and calendar RPCs. The read-only preflight verified that the whole contract is absent live; source correctly reports the missing runtime rather than an empty catalog. | open — do not deploy until issue 24 is resolved and a separate staging authorization is granted |
| 24 | P0 | The 2026-08-20 production preflight blocks 044 staging: production still has the legacy booking bypasses, no backup/PITR recovery point is available, and Auth redirect settings are unverified. | open — local 045 contains the reviewed forward-only privilege correction; staging still requires recovery, redirect verification, explicit authorization, and 044→045 atomic change-window evidence |
| 25 | P0 | Migrations 044–047 are local-only; production remains on 043 and no staging persistence/RLS/concurrency evidence exists. | open — approve isolated staging, recovery and the exact forward migration order; do not apply directly to production |
| 26 | P0 | Canonical domain, Supabase Auth Site URL and redirect allow-list are not owner-verified. | open — complete the single Cloudflare owner setup, then set the exact Staging/Production origins before login launch |
| 27 | P0 | Legal policies, media-rights records and real public catalog/price/policy content are not owner-approved/published. | open — approve and publish through governed CMS/media/catalog controls; fallbacks remain non-binding |
| 28 | P1 | Sender, payment provider callbacks, analytics/consent and malware scanner are not configured or staging-verified. | open — configure only under separate provider/environment authorization; no operational claim is made |
| 29 | P1 | Media governance stores focal points and processing state but does not yet generate derivative responsive files in an external processing pipeline. | open — configure and test an image processor/CDN on staging or retain original-only delivery with Next image optimization |
| 30 | P0 | Gate 1 recovery/PITR and restore evidence is still absent for a separately identified staging project. | awaiting-owner — the source-only migration contract, sanitized catalog validator, change runbook, and forward-only rollback decision tree are prepared; no migration may begin before a fresh recovery point, restore drill, and named restore owner exist. |
| 31 | P2 | Legal draft pages could previously be discovered in the sitemap. | locally closed — draft fallback pages are `noindex,nofollow` and excluded from sitemap; governed owner approval/publication remains required under issue 27. |
| 32 | P2 | Android/iPhone responsive evidence previously covered only 390px and a mobile overflow subset. | partially closed locally — 390/768/1440 geometry and reduced-motion regression coverage plus keyboard/mobile rendering safeguards are added; physical-device screen-reader and final performance measurements remain required. |
| 33 | P0 | The 2026-08-26 production read-only check found no PITR/physical backup and no staging branch, while 044–047 remain pending. | open — recovery/restore evidence and a separate staging environment are launch blockers; see `PRODUCTION_LAUNCH_CLOSURE_2026-08-26.md`. |
| 34 | P0 | The current public response at both hostnames does not match this release's response-header contract, and no approved Cloudflare Worker deployment has been verified. | open — stage and deploy the approved release only after recovery and Staging acceptance are complete. |
| 35 | P0 | Supabase preview branching requires Pro for this organization (`402 entitlement_required`). | open — do not purchase automatically; use a separate free disposable project after the recovery gate, or approve an upgrade later if operationally preferred. |
| 36 | P0 | A full logical database backup/restore drill is prepared, but the reported secure Production variable is not visible in the Process, Windows User or Windows Machine scope of the actual executor. | blocked — the sanitized Management API preflight proves the exact healthy Production ref, one distinct healthy Staging candidate and zero leftover restore targets. The fail-closed probe preserved that state. Make `HEBA_LAUNCH_PRODUCTION_DATABASE_URL` available to the Codex executor itself, never chat/files/logs; the runner now forces a database read-only preflight before target creation and dump. |
| 37 | P0 | Cloudflare account/GitHub connection/domain nameserver change and protected Staging have not been configured. | awaiting-owner — complete `OWNER CLOUDFLARE SETUP — ONE CONSOLIDATED REQUEST`; no login credential or provider token is requested. |
| 38 | P0 | No configured outbound-email provider or critical-monitoring integration was detected by configuration-name inspection; source defaults email delivery to disabled. | awaiting-owner — approve/configure the recommended provider choices or an existing equivalent, then prove staging delivery and a non-destructive alert. |
| 39 | P1 | The requested primary filename `hebaelsherif(2).zip` was absent; the available `hebaelsherif.zip` contains unsafe private/generated entries in addition to matching source. | locally contained — 413 tracked source files matched byte-for-byte; the archive was not extracted or imported, and packaging gates continue to forbid its private/generated entries. |
| 40 | P1 | Five 2026-08-27 evidence documents named by the execution prompt were absent at intake. | locally closed — the full required evidence set now exists and separates PASS-local, failed/corrected, environment-blocked and owner/provider-required states; missing live evidence is not relabeled as pass. |
| 41 | P1 | Homepage image selection and rights metadata cannot yet provide owner-grade media assignment until migration 046 is accepted on Staging. | environment-blocked — retain existing rights-safe assets and do not create a parallel pre-046 schema or fake rights records. |
| 42 | P1 | Formal guided-path assessment versioning still requires a dedicated forward-only schema after Contact 048, Testimonials 049, Press 050 and Resources 051. | locally closed — migration 052, `/admin/assessments` and `/start-here` now provide immutable published versions, structured question/option/result mapping, safe catalog targets, atomic permission/audit publication and ephemeral browser-only answers. Live RLS/cron/persistence remains `STAGING EXTERNAL GATE`. |
| 43 | P1 | Bundle, VIP and free-resource products have checkout primitives but no dedicated governed public discovery/detail programme in the current route matrix. | locally closed — migration 053 adds atomic permission/audit-backed publication and bundle composition, `/programs` provides published-only list/detail consumers, VIP requires plan parity, free resources link to a published Hub item, and checkout revalidates readiness. Provider persistence/RLS evidence remains `STAGING EXTERNAL GATE`. |
| 44 | P2 | The in-app interactive browser runtime failed to initialize locally with a missing kernel-assets path. | tooling-blocked — Playwright production-server E2E and 1440/390 screenshots remain available and passed; do not treat this as authenticated Staging browser evidence. |
| 45 | P1 | The first current vinext Worker run pressed the start-journey buttons before client hydration and failed 2/52 keyboard assertions. | closed locally — a real client-effect readiness signal replaced timing assumptions; type/lint/source checks and the full rebuilt Worker suite passed 52/52 with retries disabled. |
| 46 | P0 | The secure logical-backup source and all Staging database/application/provider variables are absent from the current executor process. | awaiting-owner — expose the rotated Session-pooler URL only to the actual Codex executor, or run the hidden interactive recovery script in the repository terminal. Staging Supabase/Resend/Sentry inputs follow only in their provider secret stores, never chat or Git. |
| 47 | P0 | A Production database password was disclosed in chat, and the supplied direct endpoint is IPv6-only/unreachable from this executor. | awaiting-owner — the disclosed password was not used and must be rotated in Supabase. Use the new Session pooler template on port 5432 through the hidden interactive runner; never paste the new password into chat. |
| 48 | P0 | Production and Staging previously occupied both active Supabase Free slots. | capacity closed / external gate remains — a sanitized inventory now shows only the healthy Production project and no restore target. Recovery and creation of a new separate Staging project are deferred as `STAGING EXTERNAL GATE`; no paid upgrade or Production write occurred. |
| 49 | P0 | Public Contact previously wrote directly with the anonymous browser client, accepted free-text purpose without recorded consent, and had no durable cross-instance abuse limit; Admin Inbox handling could partially write and expose message data in audit metadata. | locally closed — migration 048, the Server Action, governed form and atomic Admin RPC now enforce validation, consent, dual-scope throttling, permission checks and PII-minimized audit. Provider persistence/RLS/rate-limit proof remains `STAGING EXTERNAL GATE`. |
| 50 | P1 | Testimonials had purchase verification and basic moderation but lacked explicit text-publication consent, a dedicated public route, consent-evidenced public RLS and atomic moderation/feature audit. | locally closed — migration 049, the dashboard submission, atomic Admin RPC, `/testimonials` and Home consumer require paid-source verification, publication consent and separate first-name consent. Live persistence/RLS remains `STAGING EXTERNAL GATE`. |
| 51 | P1 | Press/appearance claims lacked a governed source, explicit independence classification, rights-aware image gate, public route and operational Admin surface. | locally closed — migration 050, `/admin/press`, `/press` and the optional Home section now provide source-linked classification, atomic permission/audit CRUD, schedule/preview and truthful empty behavior. Owner facts and live persistence remain `STAGING EXTERNAL GATE`/content input. |
| 52 | P1 | Resources had no unified public Hub, structured accessible-media contract, rights/published-related gate, Admin programme or search/Home integration. | locally closed — migration 051, `/admin/resources`, `/resources` and detail routes implement draft-safe authoring, strict publish readiness, atomic permission/audit CRUD, topic/type/duration filters, transcript/captions, structured data and governed discovery. Live RLS/Storage/cron remain `STAGING EXTERNAL GATE`. |
| 53 | P1 | Newsletter signup was not a governed public journey: legacy browser-direct writes lacked durable consent/rate-limit evidence, Admin could change subscription state directly, and opening an unsubscribe URL mutated data on GET. | locally closed — migration 055, server-mediated intake and explicit-confirm unsubscribe now enforce consent, privacy-safe throttling, service-only atomic RPCs and minimized audit. Admin cannot resubscribe without customer consent and legacy consentless rows are visibly send-ineligible. Live persistence and email delivery remain `STAGING EXTERNAL GATE`. |
| 54 | P1 | Admin Security displayed hard-coded green claims without inspecting the current environment, treated failed session reads as an empty list, and revoked sessions separately from audit writes. | locally closed — migration 056 and the rebuilt Security Center separate live/local/configured/unverified/failed evidence, expose only aggregate privacy-safe telemetry, preserve read failures, and make own-session revocation plus audit/security event atomic. Live provider evidence remains `STAGING EXTERNAL GATE`. |
| 55 | P1 | The shared Admin reader and feature-flag reader converted configured-source failures into empty data or locally enabled defaults; System also described an unavailable event stream as healthy silence and lacked provider-name readiness. | locally closed — Admin list failures now cross a sanitized boundary, optional flags fail closed, System preserves read state and reports Supabase/URL/Resend/Sentry/scanner/Staging configuration without values or live claims. Provider connectivity and delivery remain external acceptance evidence. |
| 56 | P1 | Operational settings used multiple independent writes plus a separate audit insert, while Contact reply outbox rows had no governed sender/claim/finalize lifecycle and broad Admin write access could bypass delivery state. | locally closed — migration 057 makes settings atomic, revokes direct outbox mutation, and adds permission-checked claim/finalize RPCs with locks, bounded retries and minimized audit. The Worker-safe Resend adapter uses idempotency, bounded response parsing and safe errors; real provider delivery remains `STAGING EXTERNAL GATE`. |
| 57 | P1 | Customer directory search interpolated input into a PostgREST OR filter and hid read failures as empty results; Customer 360 performed eight unbounded reads without an explicit page permission/access audit, while note/tag writes could commit separately from audit and had no removal/recovery controls. | locally closed — migration 058 and the rebuilt `/admin/users` routes add parameterized bounded service-only reads, `users.view` rechecks, PII-access audit, fail-closed states, and atomic `users.manage` note/tag lifecycle with archive/restore/remove. Live RLS/RPC persistence remains `STAGING EXTERNAL GATE`. |
