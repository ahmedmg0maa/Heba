{
  "name": "heba-elsherif-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "type-check": "tsc --noEmit",
    "audit:routes": "node scripts/audit-routes.mjs",
    "audit:ux": "node scripts/audit-ux.mjs",
    "audit:colors": "node scripts/audit-colors.mjs",
    "audit:security": "node scripts/audit-security.mjs",
    "audit:admin": "node scripts/audit-admin.mjs",
    "audit:db": "node scripts/audit-db.mjs",
    "audit:launch": "node scripts/audit-launch.mjs",
    "check:deploy": "pnpm type-check && pnpm lint && pnpm build && pnpm audit:routes && pnpm audit:ux && pnpm audit:colors && pnpm audit:security && pnpm audit:admin && pnpm audit:db && pnpm audit:launch"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
