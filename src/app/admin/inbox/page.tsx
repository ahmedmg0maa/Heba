import type{Metadata}from'next';import{adminList}from'@/lib/data/cms';import{Card,CardTitle}from'@/components/ui/Card';import{EmptyState}from'@/components/ui/EmptyState';import{MessageWorkspace,SubscriberControls}from'@/components/admin/InboxControls'
export const metadata:Metadata={title:'الرسائل والقائمة البريدية — الإدارة'}
type Message={id:string;name:string;email:string;phone:string|null;subject:string;message:string;status:string;priority:string;assigned_to:string|null;is_spam:boolean;created_at:string;contact_message_notes:{id:string;note:string;created_at:string}[]};type Subscriber={id:string;email:string;status:string;created_at:string}
export default async function AdminInboxPage(){const[messages,subscribers,roles,outbox]=await Promise.all([adminList<Message>('contact_messages','id,name,email,phone,subject,message,status,priority,assigned_to,is_spam,created_at,contact_message_notes(id,note,created_at)',{orderBy:'created_at',limit:300}),adminList<Subscriber>('newsletter_subscribers','id,email,status,created_at',{orderBy:'created_at',limit:500}),adminList<{user_id:string;role:string}>('admin_roles','user_id,role',{limit:100}),adminList<{id:string;to_email:string;subject:string;status:string;created_at:string;last_error:string|null}>('email_outbox','id,to_email,subject,status,created_at,last_error',{orderBy:'created_at',limit:100})]),admins=roles.map(role=>({id:role.user_id,label:role.role})),date=new Intl.DateTimeFormat('ar-EG',{day:'numeric',month:'short',year:'numeric',hour:'numeric',minute:'2-digit'});return <div className="mx-auto max-w-6xl space-y-8"><header><p className="text-sm font-bold text-antique-gold">مركز التواصل</p><h1 className="text-3xl font-bold text-deep-teal">الرسائل والقائمة البريدية</h1><p className="text-text-soft">إسناد وملاحظات وأولوية وردود محفوظة بأمان في outbox قبل أي إرسال خارجي.</p></header><section className="space-y-4"><h2 className="text-2xl font-bold text-deep-teal">رسائل التواصل</h2>{!messages.length?<EmptyState title="لا رسائل" description="تظهر رسائل نموذج التواصل هنا."/>:<div className="space-y-4">{messages.map(message=><Card key={message.id} className={message.is_spam?'opacity-60':''}><div className="flex flex-wrap justify-between gap-4"><div><h3 className="font-bold text-deep-teal">{message.subject||'بدون عنوان'}</h3><p className="text-sm text-text-soft">{message.name} · <span dir="ltr">{message.email}</span></p><time className="text-xs text-taupe">{date.format(new Date(message.created_at))}</time></div><span className="rounded-full bg-antique-gold/15 px-3 py-1 text-xs font-bold text-deep-teal">{message.priority}</span></div><p className="mt-4 whitespace-pre-wrap rounded-xl bg-ivory/55 p-3 text-sm leading-loose">{message.message}</p><div className="mt-4"><MessageWorkspace message={{id:message.id,status:message.status,priority:message.priority,assignedTo:message.assigned_to,spam:message.is_spam,notes:message.contact_message_notes??[]}} admins={admins}/></div></Card>)}</div>}</section><div className="grid gap-6 lg:grid-cols-2"><Card><CardTitle>المشتركات</CardTitle><div className="mt-4 space-y-3">{subscribers.map(subscriber=><div key={subscriber.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg bg-ivory/50 p-3"><div><p dir="ltr">{subscriber.email}</p><p className="text-xs text-taupe">{subscriber.status}</p></div><SubscriberControls id={subscriber.id} status={subscriber.status}/></div>)}</div></Card><Card><CardTitle>صندوق البريد الصادر</CardTitle><p className="mt-1 text-xs text-text-soft">الحالة disabled تعني أن الرسالة محفوظة ولم تغادر المنصة لأن المزود غير مهيأ.</p><div className="mt-4 space-y-3">{outbox.map(row=><div key={row.id} className="rounded-lg bg-ivory/50 p-3 text-sm"><div className="flex justify-between gap-3"><b>{row.subject}</b><span>{row.status}</span></div><p dir="ltr" className="text-xs text-taupe">{row.to_email}</p>{row.last_error&&<p className="text-xs text-burgundy">{row.last_error}</p>}</div>)}</div></Card></div></div>}
