import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'دخول المتعلّمة' }

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
