'use client'
import{useState}from'react'
import{scheduleArticle}from'@/lib/actions/cms'
import{Button}from'@/components/ui/Button'
export function ArticleScheduleControl({id,status,publishAt}:{id:string;status:string;publishAt:string|null}){const[busy,setBusy]=useState(false),[message,setMessage]=useState<string|null>(null);return <form className="grid min-w-64 gap-2 rounded-xl border border-line bg-ivory/35 p-3" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),r=await scheduleArticle(id,String(f.get('status')),String(f.get('publish_at')||'')||null);setMessage(r.ok?'حُفظت حالة النشر.':r.error);setBusy(false)}}><select name="status" defaultValue={status} className="min-h-9 rounded-lg border border-line bg-surface-raised px-2 text-sm text-ink"><option value="draft">مسودة</option><option value="scheduled">مجدول</option><option value="published">منشور</option><option value="archived">مؤرشف</option></select><input name="publish_at" type="datetime-local" defaultValue={publishAt?.slice(0,16)??''} className="min-h-9 rounded-lg border border-line bg-surface-raised px-2 text-sm text-ink"/><Button type="submit" size="sm" disabled={busy}>حفظ الجدولة</Button>{message&&<p className="text-xs text-deep-teal">{message}</p>}</form>}
