'use client'
import{useState}from'react';import{addCustomerNote,addCustomerTag}from'@/lib/actions/crm';import{Button}from'@/components/ui/Button'
export function Customer360Actions({userId}:{userId:string}){const[message,setMessage]=useState<string|null>(null),[busy,setBusy]=useState(false);const input='min-h-10 rounded-xl border border-line bg-surface-raised px-3 text-sm text-ink';return <div className="grid gap-3 md:grid-cols-2"><form className="flex gap-2" onSubmit={async e=>{e.preventDefault();setBusy(true);const r=await addCustomerNote(userId,String(new FormData(e.currentTarget).get('note')));setMessage(r.ok?'حُفظت الملاحظة.':r.error);if(r.ok)e.currentTarget.reset();setBusy(false)}}><input name="note" placeholder="ملاحظة داخلية" className={`flex-1 ${input}`}/><Button type="submit" size="sm" disabled={busy}>إضافة</Button></form><form className="flex gap-2" onSubmit={async e=>{e.preventDefault();setBusy(true);const r=await addCustomerTag(userId,String(new FormData(e.currentTarget).get('tag')));setMessage(r.ok?'حُفظ الوسم.':r.error);if(r.ok)e.currentTarget.reset();setBusy(false)}}><input name="tag" placeholder="وسم العميلة" className={`flex-1 ${input}`}/><Button type="submit" size="sm" disabled={busy}>وسم</Button></form>{message&&<p className="text-xs text-deep-teal md:col-span-2">{message}</p>}</div>}
